# Lecture Map: 314–450

This map uses the exact lecture numbers and titles from `local://async-lecture-list.txt`. A course mechanism is not automatically a StubHub design: first keep the distributed-systems problem, then inspect the current owner, durable record, guard, and proof. `Title-only` marks an inference about a video whose title does not establish its implementation details.

## Classification legend

- **KEEP** — study the concept and trace the named current code.
- **TRANSLATE** — study the problem, then use the current replacement rather than the course technology or boundary.
- **SKIP** — do not reproduce obsolete, product-specific, redundant, or needless machinery.
- **GAP** — study the concept as an explicit exercise because implementation or proof is incomplete.

## 314–349

| Lecture | Classification | Plain reason / current replacement | Exact evidence | Learner action | Module |
|---|---|---|---|---|---|
| 314 — Reusable NATS Listeners | TRANSLATE | Reusable listener behavior is useful, but the current consumer is a concrete Redis Streams function rather than a NATS listener hierarchy. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`, `processEntry`, `readNewEntries` | Learn the listener responsibilities, then trace Redis group reads and ACK timing. | [03](./03-durable-publication-and-consumption.md) |
| 315 — The Listener Abstract Class | SKIP | The repository has one concrete order-event consumer; an abstract base class would add indirection without a second implementation. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; `docs/system_design/events.md#Topology` | Skip the class shape; explain why the focused function is enough. | [01](./01-typed-event-contracts.md) |
| 316 — Extending the Listener | SKIP | There is no current listener subclass to extend; new event behavior belongs in a concrete validated flow. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/tickets/schemas.ts#orderEventSchema` | Skip inheritance and name the concrete extension point only if a real consumer appears. | [01](./01-typed-event-contracts.md) |
| 317 — Quick Refactor | TRANSLATE | Refactoring toward a readable flow applies, but current code uses focused functions and local closures instead of course classes. | `tickets/src/orders/order-events-consumer.ts#processEntries`, `consumeAvailableEntries`; `orders/src/workers.ts#scan` | Compare responsibilities, then preserve the current concrete dependency direction. | [03](./03-durable-publication-and-consumption.md) |
| 318 — Leveraging TypeScript for Listener Validation | KEEP | Compile-time event types and runtime validation both matter before untrusted broker data is used. | `tickets/src/tickets/ticket.ts#OrderEvent`; `tickets/src/tickets/schemas.ts#orderEventSchema`; `tickets/src/orders/order-events-consumer.ts#parseEvent` | Trace type declaration to `safeParse`, then state why TypeScript alone cannot validate Redis input. | [01](./01-typed-event-contracts.md) |
| 319 — Subjects Enum | TRANSLATE | NATS subjects become the explicit `orders.events` stream plus validated `eventType`; a central enum is not needed. | `tickets/src/orders/order-events-consumer.ts#stream`; `tickets/src/tickets/schemas.ts#orderEventSchema`; `docs/system_design/events.md#Topology` | Replace “subject enum” with stream name plus concrete event-type validation. | [01](./01-typed-event-contracts.md) |
| 320 — Custom Event Interface | KEEP | A small event envelope makes identity, type, versions, time, and payload explicit. | `tickets/src/tickets/ticket.ts#OrderEvent`; `orders/src/messaging/order-event-publication.ts#OrderEventPublication`; `docs/system_design/events.md#Shared envelope` | Recite the envelope fields and distinguish `messageId` from Redis entry ID. | [01](./01-typed-event-contracts.md) |
| 321 — Enforcing Listener Subjects | TRANSLATE | The listener accepts only the two supported event types through Zod, not through a NATS subject generic. | `tickets/src/tickets/schemas.ts#orderEventSchema`; `tickets/src/orders/order-events-consumer.ts#parseEvent` | Predict what an unknown `eventType` does and follow poison handling. | [01](./01-typed-event-contracts.md) |
| 322 — Quick Note: readonly in TypeScript | SKIP | **Title-only:** the title names a language feature, but no current async contract requires reproducing the course example. | **Title-only uncertainty:** no current symbol proves the lecture's exact use; current contract evidence is `tickets/src/tickets/ticket.ts#OrderEvent`. | Skip unless the video supplies a general TypeScript lesson you separately need. | [01](./01-typed-event-contracts.md) |
| 323 — Enforcing Data Types | KEEP | TypeScript describes trusted values while Zod checks the actual JSON crossing the broker boundary. | `tickets/src/tickets/schemas.ts#orderEventSchema`; `tickets/src/orders/order-events-consumer.ts#parseEvent` | Trace malformed JSON and invalid fields to the null/poison path. | [01](./01-typed-event-contracts.md) |
| 324 — Where Does this Get Used? | KEEP | The contract is used at the Tickets Redis consumer and then by the guarded repository transition. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce` | Draw the short path: stream entry → parse → transaction → ACK. | [01](./01-typed-event-contracts.md) |
| 325 — Custom Publisher | TRANSLATE | A publisher responsibility exists, but it is a concrete `XADD` function over the Orders publication ledger. | `orders/src/workers.ts#publishOrderEventPublication`; `orders/src/messaging/order-event-publication-repo.ts#listDueOrderEventPublications` | Identify what is loaded from SQLite before Redis publication. | [03](./03-durable-publication-and-consumption.md) |
| 326 — Using the Custom Publisher | TRANSLATE | The worker invokes the concrete publisher for due rows; there is no reusable NATS publisher object. | `orders/src/workers.ts#scanOrderEventPublications`, `publishOrderEventPublication` | Follow one row through `XADD`, progress marking, and retry recording. | [03](./03-durable-publication-and-consumption.md) |
| 327 — Awaiting Event Publication | KEEP | `XADD` must finish before publication progress is recorded, and the caller must await that boundary. | `orders/src/workers.ts#publishOrderEventPublication`; `orders/src/messaging/order-event-publication-repo.ts#markOrderEventPublished` | Name the crash window between append and progress. | [03](./03-durable-publication-and-consumption.md) |
| 328 — Common Event Definitions Summary | TRANSLATE | The useful summary is a small concrete shared envelope; current facts are only `order.completed` and `order.expired`. | `docs/system_design/events.md#Shared envelope`, `#order.completed version 1`, `#order.expired version 1`; `tickets/src/tickets/ticket.ts#OrderEvent` | Summarize the two accepted facts without inventing ticket or order-created events. | [01](./01-typed-event-contracts.md) |
| 329 — Updating the Common Module | SKIP | The current contract is defined in the owning service files and design document; no common event module should be added merely to mirror the course. | `tickets/src/tickets/ticket.ts#OrderEvent`; `orders/src/messaging/order-event-publication.ts#OrderEventPublication`; `docs/system_design/events.md#Schema evolution` | Skip a speculative shared package and point to the concrete owners. | [01](./01-typed-event-contracts.md) |
| 330 — Restarting NATS Play | TRANSLATE | Restart recovery is important, but Redis persistence, durable ledgers, pending entries, and startup scans replace the NATS exercise. | `orders/src/workers.ts#startWorkers`; `tickets/src/orders/order-events-consumer.ts#recoverPending`; `infra/k8s/redis/deployment.yaml#redis` | Predict what each process rereads after restart. | [03](./03-durable-publication-and-consumption.md) |
| 331 — Publishing Ticket Creation | SKIP | Ticket creation is not an accepted cross-service fact in this project; Orders publishes only terminal Order facts. | `docs/system_design/events.md#Explicit non-events`; `docs/system_design/events.md#Topology`; `tickets/src/tickets/ticket-repo.ts#createTicket` | Do not add `ticket.created`; explain why current consumers do not need it. | [01](./01-typed-event-contracts.md) |
| 332 — More on Publishing | TRANSLATE | Publishing mechanics apply, but the current mechanism is Redis `XADD` from a durable publication row. | `orders/src/workers.ts#publishOrderEventPublication`; `orders/src/messaging/README.md#durable event publication ledger` | Trace the stable `messageId` and the append-before-progress order. | [03](./03-durable-publication-and-consumption.md) |
| 333 — NATS Client Singleton | SKIP | A process-wide NATS singleton is not the current design; Orders lazily owns one Redis client and Tickets creates its consumer client. | `orders/src/workers.ts#redis`; `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer` | Skip singleton architecture; identify each client lifecycle owner. | [03](./03-durable-publication-and-consumption.md) |
| 334 — Node Nats Streaming Installation | SKIP | NATS Streaming is not a dependency or broker here. | `docs/service-design/async-systems/index.md#Current translation in one view`; `infra/k8s/redis/deployment.yaml#redis` | Do not install NATS; use the existing Redis 7 stream. | [03](./03-durable-publication-and-consumption.md) |
| 335 — Remember Mongoose? | TRANSLATE | The underlying persistence lesson transfers, while SQLite rows, constraints, and guarded SQL replace Mongoose documents and refs. | `orders/src/database.ts#database`, `withTransaction`; `orders/src/orders/order-repo.ts#completePurchase`; `orders/migrations/006_rebuild_orders.sql` | Translate a document operation into a row query plus transaction. | [02](./02-orders-boundary-and-reservation.md) |
| 336 — TS Error: Did you forget to include void in your type argument | SKIP | **Title-only:** this is a course-specific compiler detour, not an async contract or current runtime rule. | **Title-only uncertainty:** no current symbol proves the lecture's exact error; nearby current async types are `tickets/src/tickets/ticket.ts#OrderEvent`. | Skip the incidental error unless encountered independently. | [01](./01-typed-event-contracts.md) |
| 337 — Singleton Implementation | SKIP | No singleton implementation is required for current event delivery; durable rows and explicit client ownership provide the needed behavior. | `orders/src/workers.ts#redis`; `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; `docs/system_design/events.md#Topology` | Skip the singleton and explain the lifecycle boundary instead. | [03](./03-durable-publication-and-consumption.md) |
| 338 — Accessing the NATS Client | TRANSLATE | Accessing a broker client remains a concern, but current code accesses the scoped Redis client directly in worker/consumer functions. | `orders/src/workers.ts#publishOrderEventPublication`; `tickets/src/orders/order-events-consumer.ts#processEntry` | Identify which function can call Redis and when the connection exists. | [03](./03-durable-publication-and-consumption.md) |
| 339 — Graceful Shutdown | KEEP | Shutdown order narrows uncertain work windows while durable rows and pending entries preserve recovery. | `orders/src/index.ts#stop`; `orders/src/workers.ts#stopWorkers`; `tickets/src/index.ts#shutdown` | Trace HTTP stop → worker drain → Redis close for both services. | [03](./03-durable-publication-and-consumption.md) |
| 340 — Successful Listen! | TRANSLATE | Listening succeeds through a Redis consumer group and `XREADGROUP`, not a NATS subscription. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`, `readNewEntries`; `tickets/src/orders/order-events-consumer.ts#group` | Explain group creation, new-entry reads, and pending recovery. | [03](./03-durable-publication-and-consumption.md) |
| 341 — Ticket Update Publishing | SKIP | `ticket.updated` is deliberately absent; Ticket changes are owned locally and no accepted consumer needs this fact. | `docs/system_design/events.md#Explicit non-events`, `#Schema evolution`; `tickets/src/tickets/ticket-repo.ts#updateTicketPrice` | Do not create a Ticket update event; name the current local command instead. | [01](./01-typed-event-contracts.md) |
| 342 — Failed Event Publishing | KEEP | A failed append must leave the durable publication and record retry state rather than silently lose the fact. | `orders/src/workers.ts#publishOrderEventPublication`; `orders/src/messaging/order-event-publication-repo.ts#recordOrderEventPublicationFailure` | Predict the SQLite row after Redis is unavailable. | [03](./03-durable-publication-and-consumption.md) |
| 343 — Handling Publish Failures | KEEP | Connection and append failures are persisted for later scans; the worker does not claim publication succeeded. | `orders/src/workers.ts#scanOrderEventPublications`; `orders/src/messaging/order-event-publication-repo.ts#recordOrderEventPublicationFailure` | Trace one failure to `next_attempt_at`, `attempt_count`, and a later scan. | [03](./03-durable-publication-and-consumption.md) |
| 344 — Fixing a Few Tests | SKIP | **Title-only:** test repair is not itself a current delivery mechanism, and this map does not invent the course's test edits. | **Title-only uncertainty:** no exact test change can be inferred; current proof targets are documented in `docs/system_design/events.md#Crash windows`. | Skip the incidental repair and focus on observable behavior. | [06](./06-runtime-proof-and-failure-drills.md) |
| 345 — Redirecting Imports | SKIP | **Title-only:** import cleanup has no durable async behavior to carry into this repository. | **Title-only uncertainty:** no current async symbol establishes the lecture's exact import change; current module boundaries are `orders/src/messaging/` and `tickets/src/orders/`. | Skip unless a real local refactor requires it. | [01](./01-typed-event-contracts.md) |
| 346 — Providing a Mock Implementation | SKIP | Broker mocks are not the preferred proof here; real SQLite, Redis, HTTP, and pending-entry evidence tests the contract. | `docs/service-design/async-systems/index.md#Current translation in one view`; `docs/system_design/events.md#Consumption, acknowledgement, and crash windows` | Replace mock assertions with a durable-state prediction or runtime drill. | [06](./06-runtime-proof-and-failure-drills.md) |
| 347 — Test-Suite Wide Mocks | SKIP | Suite-wide broker mocks hide the crash windows this companion is meant to expose. | `docs/system_design/events.md#Crash windows`; `tickets/src/orders/order-events-consumer.ts#processEntry` | Do not add global mocks; identify the real Redis/SQLite evidence instead. | [06](./06-runtime-proof-and-failure-drills.md) |
| 348 — Ensuring Mock Invocations | SKIP | Invocation counts do not prove commit-before-ACK or duplicate safety; current proof needs ledger and broker state. | `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; `tickets/src/orders/order-events-consumer.ts#processEntry`; `docs/system_design/events.md#Consumption, acknowledgement, and crash windows` | Skip call-count proof and predict the receipt row plus ACK state. | [06](./06-runtime-proof-and-failure-drills.md) |
| 349 — NATS Env Variables | TRANSLATE | Runtime configuration matters, but the current variables are `REDIS_URL`, worker timing, and Tickets claim settings. | `orders/src/workers.ts#redis`, `#intervalMs`; `tickets/src/orders/order-events-consumer.ts#claimIdleMs`, `#claimIntervalMs`; `infra/k8s/orders/deployment.yaml#env` | Find each environment setting and say which behavior it controls. | [03](./03-durable-publication-and-consumption.md) |

## 350–383

| Lecture | Classification | Plain reason / current replacement | Exact evidence | Learner action | Module |
|---|---|---|---|---|---|
| 350 — The Orders Service | KEEP | Orders owns purchase intent, Order lifecycle, payment coordination, expiration decisions, and terminal facts. | `orders/src/app.ts#app`; `orders/src/orders/order-repo.ts#columns`; `docs/system_design/events.md#Producer` | State Orders' authority before discussing transport. | [02](./02-orders-boundary-and-reservation.md) |
| 351 — Scaffolding the Orders Service | SKIP | **Title-only:** the repository already has a running Orders service; reproducing initial scaffolding adds no learning value to this map. | **Title-only uncertainty:** exact course scaffolding is unknown; current entrypoint is `orders/src/index.ts#startWorkers` and `app.listen`. | Skip setup repetition and inspect the current entrypoint. | [02](./02-orders-boundary-and-reservation.md) |
| 352 — A Touch More Setup | SKIP | **Title-only:** unspecified setup cannot be mapped to a current business rule without inventing content. | **Title-only uncertainty:** no exact video behavior is established; current service setup is `orders/src/index.ts#port`, `#startWorkers`. | Skip unless the video reveals a concept not implied by its title. | [02](./02-orders-boundary-and-reservation.md) |
| 353 — Ingress Routing Rules | TRANSLATE | Routing is relevant, but this project uses Next.js rewrites and Kubernetes Services rather than the course's ingress setup. | `client/next.config.ts#rewrites`; `infra/k8s/orders/service.yaml#Service`; `infra/k8s/tickets/service.yaml#Service` | Trace `/api/orders` to Orders and keep internal Ticket routes out of the public proxy. | [02](./02-orders-boundary-and-reservation.md) |
| 354 — Scaffolding a Few Route Handlers | TRANSLATE | HTTP handlers still form the edge, but current routes hand business work to focused workflows. | `orders/src/http/routes/create-order.ts#router.post`; `orders/src/orders/purchase-workflow.ts#startPurchase`; `orders/src/http/routes/submit-payment.ts#router.post` | Separate HTTP status mapping from the purchase/payment workflow. | [02](./02-orders-boundary-and-reservation.md) |
| 355 — Subtle Service Coupling | TRANSLATE | Orders and Tickets must cooperate, but they exchange opaque IDs and explicit commands rather than sharing persistence. | `orders/src/tickets-client.ts#reserve`; `tickets/src/http/routes/internal-ticket-reservations.ts`; `docs/system_design/events.md#Topology` | Identify immediate coupling (reservation) versus later terminal facts. | [02](./02-orders-boundary-and-reservation.md) |
| 356 — Associating Orders and Tickets | TRANSLATE | The association is an `orderId`/`ticketId` reservation plus snapshots, not a cross-service database reference. | `orders/src/orders/order-repo.ts#completePurchase`; `tickets/src/tickets/ticket-repo.ts#reserveTicket`; `tickets/src/tickets/ticket.ts#Reservation` | Explain which IDs cross the boundary and which fields are copied as snapshots. | [02](./02-orders-boundary-and-reservation.md) |
| 357 — Order Model Setup | TRANSLATE | The Order model is a SQLite row with constraints and an explicit mapper, not a Mongoose schema. | `orders/migrations/006_rebuild_orders.sql#orders`; `orders/src/orders/order.ts#OrderRow`, `toOrder`; `orders/src/orders/order-repo.ts#columns` | Read the row, mapper, and constraint as one model. | [02](./02-orders-boundary-and-reservation.md) |
| 358 — The Need for an Enum | KEEP | A finite lifecycle prevents accidental states and makes guarded transitions readable. | `orders/src/orders/order-repo.ts#enqueueTerminal`; `orders/migrations/006_rebuild_orders.sql#status CHECK`; `orders/src/orders/order.ts#OrderRow` | List valid Order statuses and the transition that owns each. | [02](./02-orders-boundary-and-reservation.md) |
| 359 — Creating an Order Status Enum | TRANSLATE | SQLite `CHECK` constraints and TypeScript unions replace a Mongoose enum declaration. | `orders/migrations/006_rebuild_orders.sql#status CHECK`; `orders/src/orders/order-repo.ts#enqueueTerminal`; `orders/src/orders/order-repo.ts#PurchaseState` | Compare compile-time union, database constraint, and guarded update. | [02](./02-orders-boundary-and-reservation.md) |
| 360 — More on Mongoose Refs | TRANSLATE | References become opaque IDs; ownership and current truth are checked by the owning service. | `orders/src/orders/order-repo.ts#findOrderByIdForUser`; `tickets/src/tickets/ticket-repo.ts#findTicketById`; `docs/system_design/events.md#Shared envelope` | Mark every cross-service ID as reference, not a shared row. | [02](./02-orders-boundary-and-reservation.md) |
| 361 — Defining the Ticket Model | TRANSLATE | Tickets has a local SQLite row and `Ticket` projection; it owns seller data, status, and lock fields. | `tickets/src/tickets/ticket.ts#TicketRow`, `Ticket`; `tickets/migrations/001_create_tickets.sql#tickets`; `tickets/src/tickets/ticket-repo.ts#ticketColumns` | Identify fields Orders may snapshot and fields only Tickets may authorize. | [02](./02-orders-boundary-and-reservation.md) |
| 362 — Order Creation Logic | TRANSLATE | Purchase starts with a durable operation, reserves authoritatively, then inserts the Order and marks the operation complete. | `orders/src/orders/purchase-workflow.ts#startPurchase`, `processPurchase`; `orders/src/orders/order-repo.ts#createPurchase`, `completePurchase` | Trace the happy path and the `processing` response. | [02](./02-orders-boundary-and-reservation.md) |
| 363 — Finding Reserved Tickets | TRANSLATE | Orders asks Tickets' internal authority to reserve/read a matching lock; it does not query Tickets' database. | `orders/src/tickets-client.ts#reserve`, `verify`; `tickets/src/tickets/ticket-repo.ts#reserveTicket`, `findReservation` | Explain why reservation is immediate and authoritative before an Order result. | [02](./02-orders-boundary-and-reservation.md) |
| 364 — Convenience Document Methods | SKIP | Plain functions over rows are clearer here; no document methods or generic model classes are needed. | `orders/src/orders/order-repo.ts#findOrderByIdForUser`, `enqueueTerminal`; `tickets/src/tickets/ticket-repo.ts#reserveTicket` | Skip method-heavy models and name the focused function that owns each action. | [02](./02-orders-boundary-and-reservation.md) |
| 365 — Order Expiration Times | KEEP | A persisted deadline is the authority for unpaid Order expiration and retry decisions. | `orders/src/orders/order-repo.ts#purchaseTtlMs`, `duePending`; `orders/migrations/006_rebuild_orders.sql#expires_at`; `orders/src/workers.ts#scanExpiration` | Distinguish persisted deadline from a browser countdown or timer. | [05](./05-expiration-without-an-expiration-service.md) |
| 366 — globalThis has no index signature TS Error | SKIP | **Title-only:** this is an incidental compiler problem, not a reusable Orders boundary rule. | **Title-only uncertainty:** exact error context is not inferable; current configuration access is `orders/src/workers.ts#positiveIntegerSetting`. | Skip unless the same compiler error appears in current work. | [02](./02-orders-boundary-and-reservation.md) |
| 367 — Test Suite Setup | SKIP | **Title-only:** test setup does not establish the service boundary or current delivery behavior. | **Title-only uncertainty:** exact course test harness is unknown; current behavior is in `orders/src/orders/purchase-workflow.ts#startPurchase`. | Skip setup repetition; use the current flow as the trace target. | [06](./06-runtime-proof-and-failure-drills.md) |
| 368 — Small Update for Value of type typeof ObjectId is not callable | SKIP | **Title-only:** a Mongoose/ObjectId compiler detour has no current SQLite replacement to implement. | **Title-only uncertainty:** exact course error is unknown; current IDs are strings in `orders/src/orders/order-repo.ts#PurchaseRow` and `tickets/src/tickets/ticket.ts#TicketRow`. | Skip the incidental error and compare string IDs with database constraints. | [02](./02-orders-boundary-and-reservation.md) |
| 369 — Asserting Tickets Exist | GAP | Existence and not-found behavior matter, but this companion does not claim a course-style test proves them; make the HTTP/SQLite outcome observable. | `tickets/src/tickets/ticket-repo.ts#readTicketById`, `reserveTicket`; `orders/src/tickets-client.ts#reserve` maps `ticket_not_found`. | Predict the 404/not-found result and inspect the persisted rows in a drill. | [06](./06-runtime-proof-and-failure-drills.md) |
| 370 — Asserting Reserved Tickets | GAP | Reservation ownership is implemented, but a dedicated contention/replay proof is an explicit current gap. | `tickets/src/tickets/ticket-repo.ts#reserveTicket`, `findReservation`; `docs/system_design/events.md#Explicit gaps and non-claims` | Exercise same-order replay, changed deadline conflict, and another-order contention. | [06](./06-runtime-proof-and-failure-drills.md) |
| 371 — Testing the Success Case | GAP | The purchase success path exists, but the map cannot claim the external test has run; prove durable reservation, snapshots, and Order state yourself. | `orders/src/orders/purchase-workflow.ts#processPurchase`; `orders/src/orders/order-repo.ts#completePurchase`; `tickets/src/tickets/ticket-repo.ts#reserveTicket` | Predict the rows and response before running a later behavioral drill. | [06](./06-runtime-proof-and-failure-drills.md) |
| 372 — Fetching a User's Orders | KEEP | Orders owns the user-scoped list and excludes expired rows from normal history. | `orders/src/orders/order-repo.ts#listOrdersForUser`; `orders/src/http/routes/list-orders.ts#router.get` | Follow authenticated user ID into the SQL ownership predicate. | [02](./02-orders-boundary-and-reservation.md) |
| 373 — A Slightly Complicated Test | GAP | **Title-only:** the exact test is unknown, and current crash-window/ownership proof should be treated as an exercise rather than claimed coverage. | **Title-only uncertainty:** current read boundary is `orders/src/orders/order-repo.ts#listOrdersForUser`; explicit proof gaps are `docs/system_design/events.md#Explicit gaps and non-claims`. | Skip the course fixture details and write the invariant this test should prove. | [06](./06-runtime-proof-and-failure-drills.md) |
| 374 — Fetching Individual Orders | KEEP | Orders provides a user-owned detail read with an ownership predicate. | `orders/src/orders/order-repo.ts#findOrderByIdForUser`; `orders/src/http/routes/list-orders.ts#router.get` | Trace the 404 behavior for another user's Order. | [02](./02-orders-boundary-and-reservation.md) |
| 375 — Does Fetching Work? | GAP | **Title-only:** a manual/test confirmation cannot be claimed from the title; ownership and response evidence are the exercise. | **Title-only uncertainty:** current read symbol is `orders/src/orders/order-repo.ts#findOrderByIdForUser`; no course test result is asserted. | Predict owned, unowned, and missing outcomes, then verify later. | [06](./06-runtime-proof-and-failure-drills.md) |
| 376 — Cancelling an Order | SKIP | User-driven cancellation is explicitly not implemented; do not add an event or state transition to imitate the course. | `docs/system_design/events.md#Explicit gaps and non-claims`; `orders/src/orders/order-repo.ts#enqueueTerminal` supports only `order.completed` and `order.expired`. | Treat cancellation as a named product gap, not an implementation task here. | [02](./02-orders-boundary-and-reservation.md) |
| 377 — Can We Cancel? | SKIP | The answer for this repository is no current user cancellation capability; the title does not justify inventing one. | `docs/system_design/events.md#Explicit gaps and non-claims`; `orders/src/orders/order-repo.ts#Order status transitions` (no cancellation state). | Skip the course cancellation flow and record its absence. | [02](./02-orders-boundary-and-reservation.md) |
| 378 — Orders Service Events | TRANSLATE | Orders events are committed terminal facts, not every request or internal operation. | `docs/system_design/events.md#Purpose`; `orders/src/orders/order-repo.ts#enqueueTerminal`; `orders/src/messaging/order-event-publication.ts#OrderEventPublication` | List what crosses the boundary and what deliberately remains a command/internal action. | [01](./01-typed-event-contracts.md) |
| 379 — Creating the Events | TRANSLATE | Concrete version-1 `order.completed` and `order.expired` envelopes replace the course's broader event set. | `tickets/src/tickets/schemas.ts#orderEventSchema`; `tickets/src/tickets/ticket.ts#OrderEvent`; `docs/system_design/events.md#Schema evolution` | Write the minimum payload and explain why it contains only `ticketId`. | [01](./01-typed-event-contracts.md) |
| 380 — Implementing the Publishers | TRANSLATE | The publisher is a focused Orders worker that reads durable rows and appends Redis entries. | `orders/src/workers.ts#scanOrderEventPublications`, `publishOrderEventPublication`; `orders/src/messaging/order-event-publication-repo.ts#listDueOrderEventPublications` | Follow due-row selection through append and progress. | [03](./03-durable-publication-and-consumption.md) |
| 381 — Publishing the Order Creation | SKIP | `order.created` is deliberately not an accepted fact; an Order becomes externally relevant to Tickets only at terminal completion or expiration. | `docs/system_design/events.md#Explicit non-events`; `orders/src/orders/order-repo.ts#enqueueTerminal` | Do not publish creation; trace the immediate purchase workflow instead. | [01](./01-typed-event-contracts.md) |
| 382 — Publishing Order Cancellation | SKIP | User cancellation and `order.cancelled` are current gaps/non-events, not hidden requirements. | `docs/system_design/events.md#Explicit gaps and non-claims`, `#Explicit non-events`; `orders/src/orders/order-repo.ts#enqueueTerminal` | Skip cancellation publication and name the missing product decision. | [02](./02-orders-boundary-and-reservation.md) |
| 383 — Testing Event Publishing | GAP | Publication behavior is implemented, but crash-window and ACK-ordering integration tests are explicitly unproven. | `orders/src/workers.ts#publishOrderEventPublication`; `docs/system_design/events.md#Publication rule and crash windows`; `docs/system_design/events.md#Explicit gaps and non-claims` | Predict ledger state before/after append and before/after progress. | [06](./06-runtime-proof-and-failure-drills.md) |

## 384–431

| Lecture | Classification | Plain reason / current replacement | Exact evidence | Learner action | Module |
|---|---|---|---|---|---|
| 384 — Heads Up Regarding Some Mongoose TS Errors | SKIP | **Title-only:** Mongoose compiler errors are not current persistence behavior. | **Title-only uncertainty:** exact error is unknown; current persistence types are `orders/src/orders/order.ts#OrderRow` and `tickets/src/tickets/ticket.ts#TicketRow`. | Skip the incidental warning and inspect SQLite row types. | [04](./04-concurrency-versioning-and-locks.md) |
| 385 — Time for Listeners! | TRANSLATE | Listening is needed, but Redis Streams consumer functions replace NATS listener objects. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; `tickets/src/orders/order-events-consumer.ts#readNewEntries` | Trace group creation, reads, processing, ACK, and shutdown. | [03](./03-durable-publication-and-consumption.md) |
| 386 — Reminder on Listeners | TRANSLATE | The responsibility transfers directly; the API and lifecycle do not. | `tickets/src/orders/order-events-consumer.ts#processEntry`, `processEntries`; `docs/system_design/events.md#Consumption, acknowledgement, and crash windows` | State the listener invariant: no ACK before durable effect/receipt. | [03](./03-durable-publication-and-consumption.md) |
| 387 — Blueprint for Listeners | TRANSLATE | The current blueprint is parse → classify → transaction → ACK, with dead-letter-before-ACK for poison input. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce` | Write the four stages without mentioning a base class. | [03](./03-durable-publication-and-consumption.md) |
| 388 — A Few More Reminders | TRANSLATE | **Title-only:** retain only reminders that match current at-least-once processing and guarded effects. | **Title-only uncertainty:** exact reminders are unknown; current safety boundary is `tickets/src/orders/order-events-consumer.ts#processEntry` and `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`. | Re-derive duplicate, poison, and crash behavior from those symbols. | [03](./03-durable-publication-and-consumption.md) |
| 389 — Simple onMessage Implementation | TRANSLATE | `processEntry` is the current equivalent, with parsing and durable processing before `XACK`. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/orders/order-events-consumer.ts#parseEvent` | Compare a callback message handler with the current awaited function. | [03](./03-durable-publication-and-consumption.md) |
| 390 — ID Adjustment | TRANSLATE | Redis entry IDs are transport identities; the stable application `messageId` is used for deduplication. | `tickets/src/orders/order-events-consumer.ts#StreamEntry`; `tickets/src/tickets/ticket.ts#OrderEvent`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce` | Explain why a retry must preserve `messageId` even if Redis assigns another entry ID. | [01](./01-typed-event-contracts.md) |
| 391 — Ticket Updated Listener Implementation | SKIP | `ticket.updated` is not an accepted fact, so no listener should be created for it. | `docs/system_design/events.md#Explicit non-events`; `tickets/src/tickets/schemas.ts#orderEventSchema` allows only terminal Order facts. | Skip the listener and explain current Ticket ownership. | [01](./01-typed-event-contracts.md) |
| 392 — Initializing the Listeners | TRANSLATE | Startup initializes one Redis consumer group and runs pending recovery before the read loop. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`, `recoverPending`; `tickets/src/index.ts#startOrderEventsConsumer` | Trace startup order and the `BUSYGROUP` reuse path. | [03](./03-durable-publication-and-consumption.md) |
| 393 — A Quick Manual Test | GAP | Manual success is useful, but no unexecuted course-style test is claimed; use a real stream and durable ledger observation. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; `docs/system_design/events.md#Explicit gaps and non-claims` | Predict Ticket status and receipt row before a later manual run. | [06](./06-runtime-proof-and-failure-drills.md) |
| 394 — Clear Concurrency Issues | KEEP | Concurrent state transitions need transaction and predicate guards; this is a direct current concern. | `tickets/src/tickets/ticket-repo.ts#reserveTicket`, `releaseReservation`; `orders/src/database.ts#withTransaction` | Identify the competing transitions and the predicate that makes one lose safely. | [04](./04-concurrency-versioning-and-locks.md) |
| 395 — Reminder on Versioning Records | KEEP | Orders stores an aggregate version and increments it on terminal transitions; it is useful evidence, not a universal authorization key. | `orders/src/orders/order.ts#OrderRow`; `orders/src/orders/order-repo.ts#enqueueTerminal`; `docs/system_design/events.md#Aggregate version` | Distinguish aggregate version from event contract version. | [04](./04-concurrency-versioning-and-locks.md) |
| 396 — Optimistic Concurrency Control | TRANSLATE | The intent transfers to guarded SQLite updates and CAS-style retry predicates rather than a Mongoose plugin. | `orders/src/orders/order-repo.ts#schedulePurchase`; `tickets/src/tickets/ticket-repo.ts#updateTicketPrice`; `docs/service-design/async-systems/index.md#Current translation in one view` | Find the expected-state predicate in one update and describe a stale writer. | [04](./04-concurrency-versioning-and-locks.md) |
| 397 — Mongoose Update-If-Current | SKIP | The named Mongoose plugin is not used; current code expresses guards explicitly in SQL. | `orders/src/orders/order-repo.ts#schedulePurchase`; `tickets/src/tickets/ticket-repo.ts#reserveTicket`; `docs/system_design/events.md#Schema evolution` | Skip the plugin API and translate only the compare-and-set idea. | [04](./04-concurrency-versioning-and-locks.md) |
| 398 — Implementing OCC with Mongoose | TRANSLATE | Guarded writes and local transactions implement the same safety goal with SQLite. | `orders/src/database.ts#withTransaction`; `tickets/src/tickets/ticket-repo.ts#reserveTicket`; `orders/src/orders/order-repo.ts#schedulePurchase` | Rewrite one Mongoose conditional update mentally as a guarded SQL update. | [04](./04-concurrency-versioning-and-locks.md) |
| 399 — Test functions cannot both take a done callback and return something Error | SKIP | **Title-only:** a test-runner API error is incidental and not a current concurrency contract. | **Title-only uncertainty:** exact test code is unknown; current async function boundaries are `tickets/src/orders/order-events-consumer.ts#processEntry`. | Skip the incidental error. | [04](./04-concurrency-versioning-and-locks.md) |
| 400 — Testing OCC | GAP | Guarded updates exist, but dedicated contention tests are explicitly a current gap. | `tickets/src/tickets/ticket-repo.ts#reserveTicket`; `orders/src/orders/order-repo.ts#schedulePurchase`; `docs/system_design/events.md#Explicit gaps and non-claims` | Design a two-writer prediction and identify which durable update wins. | [06](./06-runtime-proof-and-failure-drills.md) |
| 401 — One More Test | GAP | **Title-only:** the test's invariant is unknown; do not claim coverage beyond the explicit code guards. | **Title-only uncertainty:** current guard evidence is `tickets/src/tickets/ticket-repo.ts#releaseReservation`; test coverage is not claimed. | State the invariant first, then make the test observable in a later drill. | [06](./06-runtime-proof-and-failure-drills.md) |
| 402 — Who Updates Versions? | KEEP | The owning local state transition increments the Order version; Tickets does not use Order version to authorize its own row. | `orders/src/orders/order-repo.ts#enqueueTerminal`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; `docs/system_design/events.md#Aggregate version` | Name the owner and update point for each version you encounter. | [04](./04-concurrency-versioning-and-locks.md) |
| 403 — Including Versions in Events | KEEP | The envelope carries both contract `eventVersion` and diagnostic `aggregateVersion`. | `orders/src/workers.ts#publishOrderEventPublication`; `orders/src/messaging/order-event-publication.ts#OrderEventPublication`; `tickets/src/tickets/schemas.ts#orderEventSchema` | Explain why the two version fields are independent. | [01](./01-typed-event-contracts.md) |
| 404 — Updating Tickets Event Definitions | TRANSLATE | Ticket-side definitions accept only the two current Order facts; no Mongoose event model is updated. | `tickets/src/tickets/ticket.ts#OrderEvent`; `tickets/src/tickets/schemas.ts#orderEventSchema`; `docs/system_design/events.md#Schema evolution` | Add no event type unless a concrete consumer and behavior exist. | [01](./01-typed-event-contracts.md) |
| 405 — Property version is missing TS Errors After Running Skaffold | SKIP | **Title-only:** this is a course build/configuration detour, not evidence that current aggregate version handling is missing. | **Title-only uncertainty:** exact error is unknown; current fields are `orders/src/messaging/order-event-publication.ts#OrderEventPublication.aggregateVersion` and `tickets/src/tickets/ticket.ts#OrderEvent.aggregateVersion`. | Skip the incidental error and inspect the explicit fields. | [04](./04-concurrency-versioning-and-locks.md) |
| 406 — Applying a Version Query | TRANSLATE | Version-aware guarded queries are useful, but current authorization uses state and exact lock owner rather than stream order or a generic version query. | `tickets/src/tickets/ticket-repo.ts#applySoldConvergence`, `applyReleaseConvergence`; `docs/system_design/events.md#Aggregate version` | Identify the exact `status` and `locked_by_order_id` predicates. | [04](./04-concurrency-versioning-and-locks.md) |
| 407 — Did it Work? | GAP | **Title-only:** an apparent successful version test cannot be claimed; current aggregate version is recorded but no high-water replay mechanism exists. | **Title-only uncertainty:** current limitation is `docs/system_design/events.md#Aggregate version` and `#Explicit gaps and non-claims`. | Treat ordered replay as an exercise and state what evidence would prove it. | [06](./06-runtime-proof-and-failure-drills.md) |
| 408 — Abstracted Query Method | SKIP | A generic query abstraction is unnecessary while focused guarded functions express the few current transitions. | `tickets/src/tickets/ticket-repo.ts#applySoldConvergence`, `applyReleaseConvergence`; `orders/src/orders/order-repo.ts#enqueueTerminal` | Prefer a direct query whose predicate names the business guard. | [04](./04-concurrency-versioning-and-locks.md) |
| 409 — Optional: Versioning Without Update-If-Current | TRANSLATE | Version evidence remains useful, but a generic high-water mark or version-ordered replay is not implemented. | `orders/src/orders/order-repo.ts#enqueueTerminal`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; `docs/system_design/events.md#Aggregate version` | Learn the alternative, then label it a deliberate future design gap rather than adding it. | [04](./04-concurrency-versioning-and-locks.md) |
| 410 — Testing Listeners | GAP | Listener crash-window and ACK-ordering tests are explicitly unproven. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `docs/system_design/events.md#Consumption, acknowledgement, and crash windows`; `docs/system_design/events.md#Explicit gaps and non-claims` | Build a prediction for crash-before-commit and crash-after-commit-before-ACK. | [06](./06-runtime-proof-and-failure-drills.md) |
| 411 — A Complete Listener Test | GAP | **Title-only:** completeness of the external listener suite is unknown; current behavior needs real Redis/SQLite evidence. | **Title-only uncertainty:** current listener is `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; proof gaps are documented in `docs/system_design/events.md#Explicit gaps and non-claims`. | Define the durable assertions before writing or running a future test. | [06](./06-runtime-proof-and-failure-drills.md) |
| 412 — Testing the Ack Call | GAP | ACK ordering is implemented in code but has no claimed integration proof. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `docs/system_design/events.md#Consumption, acknowledgement, and crash windows`; `docs/system_design/events.md#Explicit gaps and non-claims` | Prove that a failed transaction leaves the entry pending rather than ACKed. | [06](./06-runtime-proof-and-failure-drills.md) |
| 413 — Testing the Ticket Updated Listener | SKIP | There is no Ticket-updated listener because that event is deliberately absent. | `docs/system_design/events.md#Explicit non-events`; `tickets/src/tickets/schemas.ts#orderEventSchema` | Skip the test and state why no event contract exists. | [01](./01-typed-event-contracts.md) |
| 414 — Success Case Testing | GAP | **Title-only:** a success assertion is not evidence of current crash/retry safety without an executed observable scenario. | **Title-only uncertainty:** current effect boundary is `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; no run is claimed. | Predict Ticket state, receipt, and ACK state separately. | [06](./06-runtime-proof-and-failure-drills.md) |
| 415 — Out-Of-Order Events | GAP | Guards safely reject stale ownership, but generic aggregate-version ordering/high-water replay is not implemented. | `tickets/src/tickets/ticket-repo.ts#convergenceOutcome`, `applyReleaseConvergence`; `docs/system_design/events.md#Duplicate, late, and out-of-order behavior`; `docs/system_design/events.md#Explicit gaps and non-claims` | Drill a late expiration against a newer lock and record non-mutation. | [06](./06-runtime-proof-and-failure-drills.md) |
| 416 — The Next Few Videos | SKIP | **Title-only:** navigation/preview content has no independent current behavior to map. | **Title-only uncertainty:** current module sequence is documented in `docs/service-design/async-systems/index.md#Reading order`. | Skip the preview and continue with the mapped concept. | [04](./04-concurrency-versioning-and-locks.md) |
| 417 — Fixing a Few Tests | SKIP | **Title-only:** incidental test maintenance is not a current async design rule. | **Title-only uncertainty:** exact fixes are unknown; current proof gaps are `docs/system_design/events.md#Explicit gaps and non-claims`. | Skip repair details and retain the invariant being tested. | [06](./06-runtime-proof-and-failure-drills.md) |
| 418 — Listeners in the Tickets Service | TRANSLATE | Tickets owns the consumer that converges its local Ticket state from terminal Order facts. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; `docs/system_design/events.md#Consumer` | State why the consumer belongs to Tickets rather than Orders. | [03](./03-durable-publication-and-consumption.md) |
| 419 — Building the Listener | TRANSLATE | Build the concrete Redis consumer loop and parser, not an abstract listener class. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`, `consumeAvailableEntries`; `tickets/src/tickets/schemas.ts#orderEventSchema` | Trace startup, recovery, new entries, backoff, and shutdown. | [03](./03-durable-publication-and-consumption.md) |
| 420 — Strategies for Locking a Ticket | KEEP | Ticket reservation and release use SQLite immediate transactions plus exact lock-owner predicates. | `tickets/src/tickets/ticket-repo.ts#reserveTicket`, `releaseReservation`; `tickets/src/tickets/ticket.ts#TicketRow.locked_by_order_id` | Compare available, reserved-by-me, reserved-by-other, and sold outcomes. | [04](./04-concurrency-versioning-and-locks.md) |
| 421 — Reserving a Ticket | KEEP | Tickets authoritatively decides the winner synchronously for Orders. | `tickets/src/tickets/ticket-repo.ts#reserveTicket`; `tickets/src/http/routes/internal-ticket-reservations.ts`; `orders/src/tickets-client.ts#reserve` | Explain why this is an immediate command rather than a fact to await later. | [02](./02-orders-boundary-and-reservation.md) |
| 422 — Setup for Testing Reservation | GAP | **Title-only:** test setup is not current proof; reservation contention and replay remain explicit exercises. | **Title-only uncertainty:** current operation is `tickets/src/tickets/ticket-repo.ts#reserveTicket`; proof gap is `docs/system_design/events.md#Explicit gaps and non-claims`. | Specify competing requests, expected winner, and durable evidence. | [06](./06-runtime-proof-and-failure-drills.md) |
| 423 — Test Implementation | GAP | **Title-only:** no external test result is claimed; implement the prediction as an observable SQLite/HTTP drill later. | **Title-only uncertainty:** current reservation evidence is `tickets/src/tickets/ticket-repo.ts#reserveTicket`; no test execution is asserted. | Write the invariant and expected outcomes before testing. | [06](./06-runtime-proof-and-failure-drills.md) |
| 424 — Missing Update Event | SKIP | A Ticket update event is intentionally missing, not an accidental TODO; local updates remain Tickets-owned. | `docs/system_design/events.md#Explicit non-events`; `tickets/src/tickets/ticket-repo.ts#updateTicketPrice` | Do not fill this “missing event” without a real external consumer. | [01](./01-typed-event-contracts.md) |
| 425 — Private vs Protected Properties | SKIP | **Title-only:** class visibility is not needed to preserve the current function and row boundaries. | **Title-only uncertainty:** current implementation uses module-scoped functions such as `tickets/src/tickets/ticket-repo.ts#reserveTicket`, not listener subclasses. | Skip visibility mechanics and inspect actual ownership boundaries. | [04](./04-concurrency-versioning-and-locks.md) |
| 426 — Publishing While Listening | TRANSLATE | Publication and consumption coexist in the system, but as separate Orders worker and Tickets consumer flows over Redis Streams. | `orders/src/workers.ts#scanOrderEventPublications`; `tickets/src/orders/order-events-consumer.ts#readNewEntries`; `docs/system_design/events.md#Topology` | Draw the direction Orders → stream → Tickets and identify each durable boundary. | [03](./03-durable-publication-and-consumption.md) |
| 427 — Mock Function Arguments | SKIP | **Title-only:** mock argument assertions do not prove durable publication or consumer behavior. | **Title-only uncertainty:** current proof contract instead names `orders/src/messaging/order-event-publication-repo.ts#listDueOrderEventPublications` and `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`. | Skip mock details; assert durable rows, stream entries, or receipts in a later drill. | [06](./06-runtime-proof-and-failure-drills.md) |
| 428 — Order Cancelled Listener | SKIP | `order.cancelled` is not an accepted fact and there is no cancellation capability to consume. | `docs/system_design/events.md#Explicit gaps and non-claims`, `#Explicit non-events`; `tickets/src/tickets/schemas.ts#orderEventSchema` | Do not add a cancellation listener; record the product gap. | [01](./01-typed-event-contracts.md) |
| 429 — A Lightning-Quick Test | GAP | **Title-only:** a quick test cannot be claimed as current evidence; use a focused durable-state prediction instead. | **Title-only uncertainty:** current consumer effect is `tickets/src/tickets/ticket-repo.ts#applyOrderEventOnce`; no test run is asserted. | Predict one duplicate or stale-event result in writing. | [06](./06-runtime-proof-and-failure-drills.md) |
| 430 — Don't Forget to Listen! | TRANSLATE | Tickets starts its consumer before serving, but listening means Redis group recovery rather than a NATS subscription. | `tickets/src/index.ts#startOrderEventsConsumer`; `tickets/src/orders/order-events-consumer.ts#recoverPending`, `readNewEntries` | Explain what fails to converge if the consumer is not started. | [03](./03-durable-publication-and-consumption.md) |
| 431 — Rejecting Edits of Reserved Tickets | KEEP | Price edits are guarded by `status='available'`; reserved and sold Tickets cannot be edited. | `tickets/src/tickets/ticket-repo.ts#updateTicketPrice`; `tickets/src/tickets/ticket.ts#TicketStatus` | Predict the update result for available, reserved, sold, missing, and wrong-owner rows. | [04](./04-concurrency-versioning-and-locks.md) |

## 432–450

| Lecture | Classification | Plain reason / current replacement | Exact evidence | Learner action | Module |
|---|---|---|---|---|---|
| 432 — The Expiration Service | TRANSLATE | Expiration is a valuable capability, but this repository keeps it inside Orders because Orders owns deadlines and terminal transitions. | `orders/src/workers.ts#scanExpiration`; `orders/src/orders/order-repo.ts#duePending`, `enqueueTerminal`; `docs/service-design/async-systems/index.md#Current translation in one view` | Contrast a separate service with the current Orders-owned boundary without treating either as universal. | [05](./05-expiration-without-an-expiration-service.md) |
| 433 — Expiration Options | TRANSLATE | Durable due-work scans are authoritative; timers, Redis key notifications, and browser countdowns are not. | `orders/src/orders/order-repo.ts#duePending`; `orders/src/workers.ts#scanExpiration`; `docs/system_design/events.md#Explicit non-events` | Rank persisted deadline, worker scan, and UI countdown by authority. | [05](./05-expiration-without-an-expiration-service.md) |
| 434 — Initial Setup | TRANSLATE | The current setup is an Orders worker scan that runs at startup and on an interval, not a new Expiration service. | `orders/src/workers.ts#startWorkers`, `scan`; `orders/src/index.ts#startWorkers` | Trace startup scan before the recurring timer. | [05](./05-expiration-without-an-expiration-service.md) |
| 435 — Skaffold errors - Expiration Image Can't be Pulled | SKIP | **Title-only:** a historical image-pull failure does not describe a current capability and must not motivate a separate image. | **Title-only uncertainty:** exact course failure is unknown; current deployment has one `orders` image in `infra/k8s/orders/deployment.yaml#image`. | Skip the historical image issue and keep expiration within Orders. | [05](./05-expiration-without-an-expiration-service.md) |
| 436 — A Touch of Kubernetes Setup | TRANSLATE | Runtime deployment matters, but current Redis/Orders/Tickets manifests provide the needed topology without an Expiration deployment. | `infra/k8s/orders/deployment.yaml#Deployment`; `infra/k8s/tickets/deployment.yaml#Deployment`; `infra/k8s/redis/deployment.yaml#Deployment` | Identify which process owns scans and which process owns stream consumption. | [03](./03-durable-publication-and-consumption.md) |
| 437 — File Sync Setup | SKIP | **Title-only:** development file-sync mechanics are not a production expiration or delivery guarantee. | **Title-only uncertainty:** exact course sync setup is unknown; current local workflow is `skaffold.yaml#build` and current durable state is `orders/src/orders/order-repo.ts#duePending`. | Skip sync details when reasoning about restart recovery. | [05](./05-expiration-without-an-expiration-service.md) |
| 438 — Listener Creation | TRANSLATE | Expiration's downstream fact is consumed by the existing Tickets Redis consumer, not by a listener inside a new service. | `tickets/src/orders/order-events-consumer.ts#processEntry`; `tickets/src/tickets/ticket-repo.ts#applyReleaseConvergence`; `orders/src/orders/order-repo.ts#enqueueTerminal` | Follow `order.expired` from Orders publication to guarded release. | [05](./05-expiration-without-an-expiration-service.md) |
| 439 — What's Bull All About? | SKIP | Bull is unnecessary for the current scale and boundary; SQLite due rows plus an Orders worker provide durable scheduling. | `orders/src/orders/order-repo.ts#duePending`, `duePurchases`; `orders/src/workers.ts#scan`; `docs/service-design/async-systems/index.md#Current translation in one view` | Learn the scheduling problem, then reject an extra Bull queue here. | [05](./05-expiration-without-an-expiration-service.md) |
| 440 — Creating a Queue | SKIP | No separate queue is needed for expiration; the durable queue is the Orders table's due-work query. | `orders/src/orders/order-repo.ts#duePending`; `orders/migrations/006_rebuild_orders.sql#expires_at`; `orders/src/workers.ts#scanExpiration` | Replace queue creation with a query over persisted deadlines. | [05](./05-expiration-without-an-expiration-service.md) |
| 441 — Queueing a Job on Event Arrival | TRANSLATE | Work is scheduled by persisting an Order deadline and purchase operation, not by enqueueing a Bull job on an event. | `orders/src/orders/order-repo.ts#createPurchase`, `duePurchases`; `orders/src/workers.ts#scanPurchases`, `scanExpiration` | Identify the durable row that survives a process restart. | [05](./05-expiration-without-an-expiration-service.md) |
| 442 — Testing Job Processing | GAP | The worker processing path exists, but worker crash-window integration tests are explicitly unproven. | `orders/src/workers.ts#scan`, `scanExpiration`; `docs/system_design/events.md#Explicit gaps and non-claims` | Predict due-row state before and after a worker interruption. | [06](./06-runtime-proof-and-failure-drills.md) |
| 443 — Delaying Job Processing | TRANSLATE | `next_retry_at` and persisted `expires_at` provide durable delay; in-memory delayed jobs would disappear on restart. | `orders/src/orders/order-repo.ts#schedulePurchase`, `duePurchases`; `orders/src/orders/order-repo.ts#duePending` | Compare retry delay with the authoritative expiration deadline. | [05](./05-expiration-without-an-expiration-service.md) |
| 444 — Defining the Expiration Complete Event | KEEP | `order.expired` is the accepted terminal fact produced with the guarded Order transition. | `orders/src/orders/order-repo.ts#enqueueTerminal`; `orders/src/tickets-client.ts#release`; `docs/system_design/events.md#order.expired version 1` | State what the event means and what it does not authorize by itself. | [05](./05-expiration-without-an-expiration-service.md) |
| 445 — Publishing an Event on Job Processing | TRANSLATE | The worker publishes a committed terminal publication row; it does not publish directly from an ephemeral job callback. | `orders/src/workers.ts#scanExpiration`, `scanOrderEventPublications`; `orders/src/orders/order-repo.ts#enqueueTerminal`; `orders/src/messaging/order-event-publication-repo.ts#listDueOrderEventPublications` | Trace expiration transition → ledger row → Redis append. | [03](./03-durable-publication-and-consumption.md) |
| 446 — Handling an Expiration Event | KEEP | Tickets handles `order.expired` by releasing only the reservation held by that exact Order. | `tickets/src/tickets/ticket-repo.ts#convergenceOutcome`, `applyReleaseConvergence`; `tickets/src/orders/order-events-consumer.ts#processEntry` | Predict matching lock, newer lock, sold, available, and missing outcomes. | [05](./05-expiration-without-an-expiration-service.md) |
| 447 — Emitting the Order Cancelled Event | SKIP | `order.cancelled` is not accepted; expiration is the terminal release fact for the current workflow. | `docs/system_design/events.md#Explicit non-events`; `orders/src/orders/order-repo.ts#enqueueTerminal` supports only `order.completed` and `order.expired`. | Do not chain expiration into a made-up cancellation event. | [05](./05-expiration-without-an-expiration-service.md) |
| 448 — Testing the Expiration Complete Listener | GAP | Expiration convergence is implemented, but crash-window, duplicate, and stale-lock tests are explicit drills rather than claimed results. | `tickets/src/tickets/ticket-repo.ts#applyReleaseConvergence`, `applyOrderEventOnce`; `docs/system_design/events.md#Explicit gaps and non-claims` | Design a stale `order.expired` injection and name the durable evidence. | [06](./06-runtime-proof-and-failure-drills.md) |
| 449 — A Touch More Testing | GAP | **Title-only:** unspecified extra tests cannot be claimed; the current map keeps their valuable failure scenarios explicit. | **Title-only uncertainty:** relevant current code is `orders/src/workers.ts#scanExpiration` and `tickets/src/orders/order-events-consumer.ts#recoverPending`; no test execution is asserted. | Select one crash, duplicate, or restart case and write prediction/evidence/owner. | [06](./06-runtime-proof-and-failure-drills.md) |
| 450 — Listening for Expiration | KEEP | Tickets listens for the accepted `order.expired` fact through its existing consumer and applies guarded release. | `tickets/src/orders/order-events-consumer.ts#startOrderEventsConsumer`; `tickets/src/tickets/schemas.ts#orderEventSchema`; `tickets/src/tickets/ticket-repo.ts#applyReleaseConvergence` | Explain the complete expiration path and why ACK follows the local commit. | [05](./05-expiration-without-an-expiration-service.md) |

## Study playlists

### Must-watch

Study the direct current concepts first: typed event envelopes and runtime validation; Orders/Tickets ownership and authoritative reservation; SQLite transactions and guarded updates; durable publication and processed-event ledgers; Redis Streams consumer groups, pending recovery, poison handling, and ACK timing; persisted expiration deadlines; `order.completed` and `order.expired` terminal facts; and exact `lockedByOrderId` convergence.

### Translate-while-watching

Watch the course explanations of listeners, publishers, subjects, queue groups, Mongoose references, optimistic concurrency, version fields, delayed jobs, and Kubernetes setup. While watching, translate NATS to Redis Streams, Mongoose to SQLite, abstract classes to concrete functions, and a separate Expiration service/Bull queue to Orders-owned due work.

### Skip

Skip NATS installation/client singletons, abstract listener/publisher hierarchies, Ticket-created/Ticket-updated/Order-created/Order-cancelled event construction, Mongoose/ObjectId compiler detours, incidental import/test/mock repair, Bull queue creation, and historical Expiration image-pull/setup failures.

### Current gaps

Practice rather than assume proof for reservation contention; producer crash before append; producer crash after append before progress; consumer crash before commit; consumer crash after commit before ACK; exact duplicate and stale expiration delivery; poison dead-letter behavior; Redis and process restart; publication backlog and pending-entry evidence; payment-versus-expiration race; consumer-aware health; horizontal Tickets replicas; production-high-availability Redis; generic aggregate-version high-water ordering/replay; and user-driven cancellation.