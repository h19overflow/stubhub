{
  "name": "@stubhub/nats-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "publish": "tsc && node dist/publisher.js",
    "listen": "tsc && node dist/listner.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-nats-streaming": "^0.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
