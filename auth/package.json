{
  "name": "@stubhub/identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon --watch src --watch migrations --ext ts,sql --exec \"npm run build && node dist/index.js\"",
    "build": "tsc -p tsconfig.json",
    "migrate": "npm run build && node dist/database.js",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.2.1",
    "nodemailer": "^9.0.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "@types/nodemailer": "^8.0.1",
    "nodemon": "^3.1.14",
    "typescript": "^7.0.2"
  }
}
